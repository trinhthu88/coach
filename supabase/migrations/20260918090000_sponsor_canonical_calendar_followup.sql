-- SUPERSEDED — intentionally a no-op. Never applied to hosted production.
--
-- This migration originally (a) redefined sponsor_canonical_module_schedule
-- with the dynamic "policy re-interpreted on every read" schedule, (b)
-- granted that internal schedule function to every authenticated user, and
-- (c) inlined sponsor_canonical_programme_journey with SQL functionally
-- identical to get_sponsor_programme_journey (the function it already
-- delegates to).
--
-- None of that may be applied any more:
--   (a) would silently restore dynamic scheduling after
--       20260918160000_cohort_requirement_schedule made the stored cohort
--       dates (cohort_requirement_dates) the canonical schedule;
--   (b) would expose any enrollment's schedule to any signed-in user;
--   (c) adds nothing and would bypass the shared cohort journey
--       construction (and its no-module-list-label rule).
--
-- Its one scheduling intent (Training-linked dates follow the Cohort
-- calendar) is now owned by cohort_requirement_dates: dates are generated by
-- the proposal layer and edited per cohort by Admins.
--
-- The file is kept (empty) so that its version stays reserved: applying it
-- in any order — before or after 20260918160000 — changes nothing.
-- supabase/tests/cohort_requirement_schedule_test.sql and
-- src/test/migrationChain.test.ts guard the final canonical definitions.
SELECT 1;
